spring.application.name=banking-transaction-engine

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/banking_transaction_engine}
spring.datasource.username=${DB_USERNAME:vvsganeshk}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.flyway.enabled=true